x-arcane:
  icon: https://cdn.jsdelivr.net/gh/selfhst/icons@main/svg/nginx.svg
  urls:
    - https://nginx.org/
    - https://hub.docker.com/_/nginx

services:
  nginx:
    image: nginx:alpine
    ports:
      - "${NGINX_PORT:-8080}:80"
    restart: unless-stopped
